/*
 * PROJECT:     ReactOS NDIS 6 support
 * LICENSE:     GPL-2.0-or-later (https://spdx.org/licenses/GPL-2.0-or-later)
 * PURPOSE:     Encapsulation offload configuration
 *
 * The encapsulation bits and NDIS_OFFLOAD_ENCAPSULATION are declared with the
 * rest of the offload surface. This makes them reachable under the name the
 * WDK split them out to.
 */

#pragma once

#include <ndis/offload.h>
